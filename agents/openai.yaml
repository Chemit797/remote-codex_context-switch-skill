interface:
  display_name: "Codex Key & Provider Switch"
  short_description: "Swap Codex key or provider without losing history"
  default_prompt: "Use $remote-codex-provider-switch to switch my Codex key, account or relay: run the read-only check first, apply the smallest change, then verify it."

policy:
  allow_implicit_invocation: true
