interface:
  display_name: "Remote Codex Context Recovery"
  short_description: "Safely recover local Codex tasks after account changes"
  default_prompt: "Use $remote-codex-provider-switch to inventory and safely recover selected local Codex tasks after an account or remote-host change."

policy:
  allow_implicit_invocation: true
